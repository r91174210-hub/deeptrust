{
  "name": "deeptrust-blockchain",
  "version": "1.0.0",
  "description": "DeepTrustRegistry smart contract - immutable certificate anchor",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
